resources:
  - name: t_r_gitRepo
    type: GitRepo
    configuration:
      path: Varsha-Arun/nativeSteps_pipelines
      gitProvider: varshaGithub

pipelines: 
  - name: p_thanos
    steps:
      - name: t_s_bash
        type: Bash
        configuration:
          integrations:
            - name: art
        execution:
          onStart:
            - echo "test1"
